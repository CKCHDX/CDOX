// src/firewall/FirewallWidget.h
#pragma once
#include <QWidget>

class FirewallWidget : public QWidget {
    Q_OBJECT
public:
    explicit FirewallWidget(QWidget *parent = nullptr);
    // Add public API or slots for controlling the firewall here.
};
